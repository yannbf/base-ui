import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as AccordionStories from './accordion.stories';
import { Accordion } from '@base-ui/react/accordion';
import { WildCards, WildCard } from '../shared/InTheWild';
import nineUiDevShot from '../../../../../research/d-real-world-usage/_captures/9ui-dev.png';
import lumiuiDevOpenShot from '../../../../../research/d-real-world-usage/_captures/lumiui-dev-open.png';
import reuiIoOpenShot from '../../../../../research/d-real-world-usage/_captures/reui-io-open.png';

<Meta of={AccordionStories} />

# Accordion

{/* BEGIN: general */}
A set of collapsible panels with headings.

- **Import:** `import { Accordion } from '@base-ui/react/accordion';`
- **Parts:** 5 (`Accordion.Root`, `Accordion.Item`, `Accordion.Header`, `Accordion.Trigger`, `Accordion.Panel`).
- **Status:** Stable. Introduced 2024-10-30 ([#577](https://github.com/mui/base-ui/pull/577)); built directly on Collapsible's primitives rather than a parallel reimplementation (see [Collapsible](?path=/docs/disclosure-structure-collapsible--docs)).
- **ARIA pattern:** [Accordion](https://www.w3.org/WAI/ARIA/apg/patterns/accordion/), the **current** revision, which no longer recommends roving-tabindex arrow-key navigation between headers.

<Canvas of={AccordionStories.Hero} />

## Features

- **A coordination layer over Collapsible**: each `Accordion.Item` is internally one `Collapsible` whose open/close is redirected into the shared `Accordion.Root` value array.
- **Single or multiple open at once**: `multiple` (default `false`) controls whether opening one item closes the others or lets any subset stay open.
- **Animated panel height**: `--accordion-panel-height`/`--accordion-panel-width` CSS vars, driven by the same measurement engine Collapsible uses.
- **Disabled items stay discoverable**: a disabled trigger remains focusable via Tab; it just never toggles.
- **Browser find-in-page support**: `hiddenUntilFound` uses `hidden="until-found"` so collapsed panels stay searchable via Ctrl/Cmd+F.

{/* END: general */}

{/* BEGIN: when-to-use */}
## When to use

- FAQ-style content, settings panels, or any "set of collapsible panels with headings" where readers scan headings and expand only what they need.
- When exactly one section should typically be open at a time, keeping the page compact. This is the default (`multiple: false`).
- When multiple sections may reasonably be open simultaneously (e.g. comparing two FAQ answers side by side), via `multiple`.

## When not to use

- For a single show/hide region with no siblings to coordinate, use [Collapsible](?path=/docs/disclosure-structure-collapsible--docs) directly. Accordion's Root/value/`multiple` machinery buys nothing for a lone panel.
- For switching between mutually exclusive, persistent peer views (not stacked disclosure), use [Tabs](?path=/docs/navigation-tabs--docs). An accordion's panels are all part of one continuous document (any subset can be open); tabs present exactly one panel at a time in a fixed viewport region.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}
## Anatomy

```tsx
import { Accordion } from '@base-ui/react/accordion';

<Accordion.Root>
  <Accordion.Item>
    <Accordion.Header>
      <Accordion.Trigger />
    </Accordion.Header>
    <Accordion.Panel />
  </Accordion.Item>
</Accordion.Root>;
```

| Part                | Renders    | Purpose                                       | Optional?          |
| :------------------ | :--------- | :-------------------------------------------- | :----------------- |
| `Accordion.Root`    | `<div>`    | Groups all parts of the accordion             | required           |
| `Accordion.Item`    | `<div>`    | Groups a header with its corresponding panel  | required, repeated |
| `Accordion.Header`  | `<h3>`     | A heading that labels the corresponding panel | required           |
| `Accordion.Trigger` | `<button>` | Opens and closes the corresponding panel      | required           |
| `Accordion.Panel`   | `<div>`    | The collapsible panel contents                | required           |

{/* END: anatomy */}

{/* BEGIN: behavior */}
## How it works

### Single vs `multiple`

Uncontrolled via `defaultValue` (an array, default `[]`); controlled via `value`/`onValueChange`. With `multiple={false}` (the default), opening item X always replaces the whole value array with `[X]`; with `multiple={true}`, opening pushes X onto the array and closing filters it out. Any subset, including all or none, can be open.

<Canvas of={AccordionStories.OpenMultiple} />

### Disabled items

Root-level `disabled` cascades to every item; Item-level `disabled` disables just one panel. Disabled triggers stay focusable (`focusableWhenDisabled`) but never toggle.

<Canvas of={AccordionStories.DisabledItem} />

### `multiple` combined with a disabled item

A disabled item never joins the open set and is unaffected by its enabled siblings, even under `multiple`.

<Canvas of={AccordionStories.MultipleWithDisabled} />

### Animated panel height

The Panel's height is driven by `--accordion-panel-height` (`AccordionPanelCssVars`), the same measurement engine Collapsible uses.

<Canvas of={AccordionStories.AnimatedPanelHeight} />

### Browser find-in-page support

`hiddenUntilFound` uses `hidden="until-found"` instead of unmounting, so collapsed panels stay searchable via Ctrl/Cmd+F.

<Canvas of={AccordionStories.HiddenUntilFound} />

### Controlled

External `value`/`onValueChange` state, driven by buttons outside the accordion entirely.

<Canvas of={AccordionStories.ControlledValue} />

{/* END: behavior */}

{/* BEGIN: a11y */}
## Keyboard & screen reader

| Key                                      | Action                                                                                                                                                                                     |
| :--------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Tab` / `Shift+Tab`                      | Moves focus to the next/previous focusable element in native DOM tab order, including between headers, since each Trigger is an ordinary tab stop, not a roving-tabindex composite member |
| `Enter`                                  | Toggles the focused item's open state                                                                                                                                                      |
| `Space`                                  | Toggles the focused item's open state (fires on keyup, matching native button semantics)                                                                                                   |
| `ArrowUp` / `ArrowDown` / `Home` / `End` | **No accordion-specific behavior**: by design per current APG guidance ([#4965](https://github.com/mui/base-ui/pull/4965))                                                                                               |

The following story asserts this contract directly: Tab/Shift+Tab moves focus between triggers, ArrowDown does nothing, and Space toggles the focused trigger.

<Canvas of={AccordionStories.KeyboardTabFlow} />

- **ARIA**: `Trigger` gets `aria-expanded` (always present) and `aria-controls` (present only while open); `Panel` gets `role="region"` and `aria-labelledby={triggerId}`. `Root` and `Item` carry no ARIA role or state.
- **You must provide**: nothing extra. The Header→Trigger→Panel labeling chain is automatic as long as the canonical nesting is followed.
- **Known issues**: none known beyond the resolved APG-alignment PRs below.

{/* END: a11y */}

{/* BEGIN: styling */}
## Styling hooks

| Part              | Attribute / variable                                                                         | Present when                                          |
| :---------------- | :------------------------------------------------------------------------------------------- | :---------------------------------------------------- |
| `Item` / `Header` | `data-open`, `data-disabled`, `data-index`                                                   | reflects item state                                   |
| `Trigger`         | `data-panel-open` (not `data-open`), `data-disabled`                                         | trigger's associated panel is open / trigger disabled |
| `Panel`           | `data-open`, `data-disabled`, `data-starting-style`, `data-ending-style`, `data-orientation` | animation boundaries and state                        |
| `Panel`           | `--accordion-panel-height`, `--accordion-panel-width`                                        | always, drives the height/width transition            |

{/* END: styling */}

{/* BEGIN: general */}
## Choosing the right props

- **`multiple`**: set `true` to allow more than one panel open at once; defaults to `false` since a deliberate breaking change ([#3141](https://github.com/mui/base-ui/pull/3141)).
- **`hiddenUntilFound`**: use when collapsed content should still be discoverable via the browser's native find-in-page search; it structurally forces the panel to stay mounted, overriding an explicit `keepMounted={false}` with a dev warning.
- **`orientation` / `loopFocus`**: both `@deprecated`; no longer affect keyboard focus behavior following the APG update. `orientation` still drives the `data-orientation` styling hook (e.g. chevron rotation) but carries no behavioral guarantee.

{/* END: general */}

{/* BEGIN: do-dont */}
## Do / Don't

**Do**

- Use `value`/`multiple` to decide steady-state behavior up front. Switching `multiple` later is a breaking change for controlled consumers relying on a single-value array shape.

**Don't**

- Don't expect ArrowUp/ArrowDown to move focus between headers. That behavior was removed by design ([#4965](https://github.com/mui/base-ui/pull/4965)); use Tab/Shift+Tab instead.

{/* END: do-dont */}

{/* BEGIN: brand */}
## Brand guidelines

**Do**

- Keep header hierarchy a matter of size and weight — the open state never recolors text. (BR-07)
- Run the expand/collapse transition 150–300ms ease-in via the motion tokens. (BR-12)
{/* END: brand */}

{/* BEGIN: examples */}
## Examples

An `Accordion.Panel` can contain an entirely independent, nested `Accordion.Root`: each root owns its own value/context, so the two don't interfere.

<Canvas of={AccordionStories.NestedAccordion} />

## In the wild

<WildCards component="accordion">
  <WildCard
    repo="borabaloglu/9ui"
    title="9ui"
    href="https://github.com/borabaloglu/9ui/blob/HEAD/apps/www/src/components/ui/accordion.tsx"
    live="https://www.9ui.dev"
    license="MIT"
    reuse="code-ok"
    image={nineUiDevShot}
  >
    a shadcn-style `data-slot="accordion"` wrapper over `@base-ui/react/accordion`, styled with
    Tailwind border-bottom dividers per item.
  </WildCard>
  <WildCard
    repo="patrick-xin/lumi-ui"
    title="lumi-ui"
    href="https://github.com/patrick-xin/lumi-ui/blob/HEAD/packages/ui/src/components/ui/accordion.tsx"
    live="https://www.lumiui.dev"
    license="MIT"
    reuse="code-ok"
    image={lumiuiDevOpenShot}
  >
    the same one-file-per-registry-item pattern, published both as an installable registry item
    (`apps/www/registry/ui/accordion.tsx`) and inside the distributable `@lumi-ui` package.
  </WildCard>
  <WildCard
    repo="keenthemes/reui"
    title="reui"
    href="https://github.com/keenthemes/reui/blob/HEAD/registry/bases/base/ui/accordion.tsx"
    live="https://reui.io"
    license="MIT"
    reuse="code-ok"
    image={reuiIoOpenShot}
  >
    ships parallel Base UI and Radix implementations behind the same component API
    (`registry/bases/base/` vs `registry/bases/radix/`), letting consumers pick a primitive backend
    per project.
  </WildCard>
</WildCards>

{/* END: examples */}

{/* BEGIN: history */}
## Decision log — the APG-alignment removal

Two related PRs, merged the same day (2026-06-08), that removed behavior which had shipped since Accordion's 2024-10-30 launch ([#577](https://github.com/mui/base-ui/pull/577)):

- **[#4965](https://github.com/mui/base-ui/pull/4965): Align Accordion keyboard navigation with APG.** The Accordion APG pattern no longer recommends roving focus, as documented in [w3c/aria-practices#3434](https://github.com/w3c/aria-practices/pull/3434), so we removed roving focus from Accordion to align with the latest APG guidance. In its recent revisions, Accordion never used a `Composite`/roving-tabindex implementation to begin with. The PR formalizes the absence as intentional rather than a missing feature, and marks `orientation`/`loopFocus` as `@deprecated` for keyboard purposes.
- **[#4961](https://github.com/mui/base-ui/pull/4961): Remove region role from `Accordion.Root`.** In the WAI-ARIA accordion pattern, `region` is typically applied to accordion panels rather than the root container. `Accordion.Root` serves as a structural wrapper and has no accessible label of its own, so removing the region role better aligns with the APG guidance. `role="region"` already lived correctly on `Accordion.Panel`, so no functional gap resulted, only removal of a redundant/misplaced role on Root.

The removal is by design rather than a regression. Current W3C APG guidance dropped roving-focus arrow-key navigation from the Accordion pattern entirely, and we track that guidance rather than an older, more elaborate version some other libraries still implement.

- **2024-10-30**: Accordion ships ([#577](https://github.com/mui/base-ui/pull/577)), built from day one atop shared Collapsible hooks.
- **2025-beta.5**: `multiple` defaults to `false` ([#3141](https://github.com/mui/base-ui/pull/3141)), a deliberate breaking change.
- **2026-06-08**: APG alignment pair, [#4965](https://github.com/mui/base-ui/pull/4965) (roving-focus removal) and [#4961](https://github.com/mui/base-ui/pull/4961) (`role="region"` removal from Root).

{/* END: history */}

{/* BEGIN: when-to-use */}
## Related

- For a single, independent show/hide region, use [Collapsible](?path=/docs/disclosure-structure-collapsible--docs).
- For switching between mutually exclusive peer views, use [Tabs](?path=/docs/navigation-tabs--docs).

{/* END: when-to-use */}

{/* BEGIN: props */}
## API reference

### Accordion.Root

<ArgTypes of={Accordion.Root} />

### Accordion.Item

<ArgTypes of={Accordion.Item} />

### Accordion.Header

<ArgTypes of={Accordion.Header} />

### Accordion.Trigger

<ArgTypes of={Accordion.Trigger} />

### Accordion.Panel

<ArgTypes of={Accordion.Panel} />

_Full JSDoc-driven reference: [base-ui.com/react/components/accordion](https://base-ui.com/react/components/accordion#api-reference)._

{/* END: props */}
