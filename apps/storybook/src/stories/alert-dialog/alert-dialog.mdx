import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as AlertDialogStories from './alert-dialog.stories';
import { AlertDialog } from '@base-ui/react/alert-dialog';
import { WildCards, WildCard } from '../shared/InTheWild';
import nineUiDevShot from '../../../../../research/d-real-world-usage/_captures/9ui-dev.png';
import lumiuiDevOpenShot from '../../../../../research/d-real-world-usage/_captures/lumiui-dev-open.png';

# Alert Dialog

<Meta of={AlertDialogStories} />

{/* BEGIN: general */}
A dialog that requires a user response to proceed: destructive-action confirmations,
unsaved-changes warnings, and required-acknowledgment prompts that must be impossible
to dismiss by accident.

- **Import:** `import { AlertDialog } from '@base-ui/react/alert-dialog';`
- **Parts:** 9 (`Root`, `Trigger`, `Portal`, `Backdrop`, `Viewport`, `Popup`, `Title`,
  `Description`, `Close`), plus the non-element `AlertDialog.createHandle()`. Only
  `Root` and `Trigger` are original; every other part is a re-export of the
  corresponding Dialog part ([#3237](https://github.com/mui/base-ui/pull/3237)).
- **Status:** Stable. Created together with Dialog in the same founding PR
  ([#372](https://github.com/mui/base-ui/pull/372), Jun 2024).
- **ARIA pattern:** [Alert Dialog (Alert and Message Dialogs)](https://www.w3.org/WAI/ARIA/apg/patterns/alertdialog/),
  distinct from Dialog's plain Dialog (Modal) pattern.

<Canvas of={AlertDialogStories.Hero} />

## Features

- **Three axes hard-coded, at the type level**: AlertDialog is Dialog with `role`,
  `modal`, and `disablePointerDismissal` all fixed at their most conservative setting.
  `useRenderDialogRoot(props, 'alert-dialog')` derives:

  ```ts
  const isAlertDialog = mode === 'alert-dialog';
  const modal = isAlertDialog ? true : modalProp;
  const disablePointerDismissal = isAlertDialog || disablePointerDismissalProp;
  const role: 'dialog' | 'alertdialog' = isAlertDialog ? 'alertdialog' : 'dialog';
  ```

  `AlertDialogRoot.Props` **`Omit`s `modal` and `disablePointerDismissal` from
  `DialogRoot.Props` entirely**, so passing either one is a type error. If a scenario
  needs `modal={false}` or a dismissible confirmation, use plain Dialog.

- **Why a separate component instead of a `severity` prop on Dialog**: we separated
  it from Dialog for accessibility reasons
  ([#2146](https://github.com/mui/base-ui/issues/2146)), following MDN's `alertdialog`
  role. The separation is an ARIA-semantics decision, not a styling one.
- **Esc is _not_ part of the lockdown**: `useDialogRoot.ts` gates outside-press
  dismissal on `isTopmost && !disablePointerDismissal`, but gates the Escape-key
  handler only on `isTopmost`, with no `disablePointerDismissal` check at all.
  "No light dismiss" means specifically _no outside-press dismissal_, not _no
  keyboard dismissal at all_: Esc still closes an AlertDialog by default.
- **Nominally-branded handle**: `AlertDialog.createHandle()` returns an
  `AlertDialogHandle`, a subclass of `DialogHandle` carrying a type-only brand so it
  can't be passed interchangeably with a `Dialog.Handle`, even though both behave
  identically at runtime.
- **Architectural consolidation**: for its first ~17 months AlertDialog had fully
  duplicated part implementations; [#3237](https://github.com/mui/base-ui/pull/3237)
  (Nov 2025) deleted ~1,600 lines of duplication in favor of the re-export shape
  described above.

{/* END: general */}

{/* BEGIN: when-to-use */}
## When to use

- Destructive-action confirmations (discard/delete), unsaved-changes warnings, and
  terms-of-service/required-acknowledgment prompts, such as the hero demo's
  "Discard draft?" confirmation.
- When the interruption must be impossible to dismiss by accident: no backdrop click,
  no outside click; only an explicit in-dialog choice (or Esc, which the app can
  still veto in `onOpenChange`).
- As the nested confirmation inside an in-progress Dialog flow, the canonical
  "close confirmation" pattern (see [Dialog](?path=/docs/overlays-dialog--docs)'s
  `NestedAlertDialogGuard`/`CloseConfirmation` stories): a regular dismissible Dialog
  for the main flow, an AlertDialog for the one "are you sure?" decision point inside it.
- Multiple/detached triggers and payload-driven content, using the same mechanism
  as Dialog's `createHandle`/`payload` kit.

## When not to use

- **vs Dialog (the primary boundary)**: use plain Dialog for anything dismissible:
  settings panels, forms the user can back out of, notification centers. Use
  AlertDialog only for the case Dialog can't express: a decision the user cannot
  escape by clicking outside. A Dialog can be made to behave like an AlertDialog by
  setting `modal` + `disablePointerDismissal` explicitly, but an AlertDialog can
  never be made dismissible.
- **vs Toast**: passive, self-clearing notifications are Toast's job. Toasts notify
  users without interrupting their workflow; when user input is required, AlertDialog
  is better suited ([#4533](https://github.com/mui/base-ui/issues/4533)).
- **Don't build a `confirm()` abstraction on top of a manager**: we declined a
  `dialogManager` API for Dialog
  ([#2802](https://github.com/mui/base-ui/issues/2802)), and the same applies here.
  Compose handles + `onOpenChange` reason-branching per confirmation instance.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}
## Anatomy

```tsx
import { AlertDialog } from '@base-ui/react/alert-dialog';

<AlertDialog.Root>
  <AlertDialog.Trigger />
  <AlertDialog.Portal>
    <AlertDialog.Backdrop />
    <AlertDialog.Viewport>
      <AlertDialog.Popup>
        <AlertDialog.Title />
        <AlertDialog.Description />
        <AlertDialog.Close />
      </AlertDialog.Popup>
    </AlertDialog.Viewport>
  </AlertDialog.Portal>
</AlertDialog.Root>;
```

| Part                         | Renders    | Purpose                                                                                                       | Optional?                                      |
| :---------------------------- | :---------- | :------------------------------------------------------------------------------------------------------------- | :---------------------------------------------- |
| `AlertDialog.Root`           | no element | Groups all parts; calls `useRenderDialogRoot(props, 'alert-dialog')`                                          | required                                       |
| `AlertDialog.Trigger`        | `<button>` | Thin re-export of `DialogTrigger`, cast to a distinct alert-dialog-flavored type                              | yes (controlled/handle modes work without one) |
| `AlertDialog.Portal`         | `<div>`    | Moves the popup subtree to `<body>` (or `container`)                                                          | required for popup parts                       |
| `AlertDialog.Backdrop`       | `<div>`    | Presentation-only; **never closes the dialog when clicked**                                                   | yes                                            |
| `AlertDialog.Viewport`       | `<div>`    | Positioning/scroll container, identical to Dialog's                                                           | yes                                            |
| `AlertDialog.Popup`          | `<div>`    | The `role="alertdialog"` surface                                                                              | required                                       |
| `AlertDialog.Title`          | `<h2>`     | Labels the dialog; auto-wires `aria-labelledby`                                                              | yes (strongly recommended)                     |
| `AlertDialog.Description`    | `<p>`      | Describes the consequence; auto-wires `aria-describedby`                                                     | yes                                            |
| `AlertDialog.Close`          | `<button>` | **The only pointer-accessible way out besides Esc**; outside-press and backdrop click never close the dialog | yes (required in practice)                     |
| `AlertDialog.createHandle()` | —          | Returns an `AlertDialogHandle` (nominally distinct from `DialogHandle`)                                       | yes                                            |

`Close` carries more weight here than in Dialog: since outside-press is always
disabled and the backdrop never closes the dialog, every docs demo renders at least
one `Close` inside the Popup.

{/* END: anatomy */}

{/* BEGIN: behavior */}
## How it works

### The destructive-confirm flow

Click the trigger, the popup opens with `role="alertdialog"` and `aria-labelledby`/
`aria-describedby` wired to Title/Description, and only an explicit choice closes it:

<Canvas of={AlertDialogStories.Hero} />

### No outside-press dismissal: the hard-coded contract

`disablePointerDismissal` is permanently `true` and cannot be overridden. Clicking the
backdrop is a documented no-op; Esc is **not** blocked by the same lockdown and closes
the dialog normally (branch on `eventDetails.reason === 'escape-key'` in `onOpenChange`
if an unconfirmed destructive action shouldn't be closeable via Esc either):

<Canvas of={AlertDialogStories.NoOutsidePressDismissal} />

### Required-acknowledgment composition

AlertDialog has no built-in validation. Compose it with ordinary form state to gate
the destructive action behind a typed confirmation phrase:

<Canvas of={AlertDialogStories.FormWithConfirmation} />

### Cross-type nesting: AlertDialog inside a Dialog

A regular, dismissible Dialog for the main flow, guarded by a controlled AlertDialog
that opens if the user tries to close a dirty form: the canonical "close
confirmation" pattern. The nested AlertDialog still counts toward the parent's
`[data-nested-dialog-open]` machinery even though it's a different component type:

<Canvas of={AlertDialogStories.NestedCloseConfirmation} />

### Controlled mode and opening from elsewhere

`open`/`onOpenChange` work with no `AlertDialog.Trigger` at all:

<Canvas of={AlertDialogStories.ControlledMode} />

Opening from a `Menu.Item`'s `onClick` is a documented composition, identical to
Dialog's "Open from a menu" pattern:

<Canvas of={AlertDialogStories.TriggerFromMenu} />

### Exit animation

`onOpenChangeComplete` fires once a CSS transition finishes; the popup stays
mounted through the exit transition:

<Canvas of={AlertDialogStories.ExitAnimation} />

### Custom render composition

`render` composes both the Trigger (a custom button component) and the Popup (a
native `<section>` instead of the default `<div>`) while every AlertDialog behavior
is preserved unchanged:

<Canvas of={AlertDialogStories.CustomRenderComposition} />

### Esc closes and returns focus

Esc is the one dismissal path still open. Closing via Esc restores focus to the
Trigger that opened the dialog:

<Canvas of={AlertDialogStories.EscFocusReturn} />

### Handle + payload reused across many triggers

One `AlertDialog.createHandle<Payload>()` shared by many detached triggers, each
with its own `payload`: a single confirmation instance reused across a list of
delete buttons:

<Canvas of={AlertDialogStories.HandleWithPayload} />

{/* END: behavior */}

{/* BEGIN: a11y */}
## Keyboard & screen reader

| Key                              | Action                                                                                                              |
| :-------------------------------- | :------------------------------------------------------------------------------------------------------------------- |
| `Tab` / `Shift+Tab`              | Moves through tabbables inside the popup; **always loops** (trapped), since `modal` can never be relaxed for AlertDialog |
| `Esc`                            | Closes the topmost open alert dialog (reason `escape-key`); **not** blocked by the pointer-dismissal lockdown      |
| `Enter`/`Space` on Trigger/Close | Native button activation                                                                                            |
| Click/tap on Backdrop            | **No-op**; the one row that most distinguishes AlertDialog from Dialog                                             |

- **ARIA wiring**: Popup `role="alertdialog"`, `aria-labelledby` ← `Title`,
  `aria-describedby` ← `Description`. Trigger: `aria-haspopup="dialog"` (stays
  `"dialog"`, not `"alertdialog"`; there is no `alertdialog` token in the ARIA spec
  for `aria-haspopup`), `aria-expanded`, `aria-controls`.
- **No `aria-modal`**: omitted for the same reason as in Dialog
  (outside elements are marked `aria-hidden`/inert instead).
- **Touch screen readers**: because `modal` is always `true`, rendering an
  `AlertDialog.Close` inside the Popup is closer to a hard requirement than Dialog's
  equivalent guidance, which only applies when a consumer opts into `modal={true}`.

{/* END: a11y */}

{/* BEGIN: general */}
## Choosing the right props

- **No `modal` prop**: cannot be set at all. If a scenario needs `modal={false}` or
  `'trap-focus'`, it needs plain Dialog, not AlertDialog.
- **No `disablePointerDismissal` prop**: permanently forced `true`. There is no way
  to make an AlertDialog instance dismissible by clicking outside.
- **`onOpenChange(open, eventDetails)`**: same `eventDetails.reason`/`.cancel()`/
  `.trigger` contract as Dialog; the reason that matters most here is distinguishing
  `escape-key` (the one dismissal path still open) from `close-press`/`trigger-press`.
- **`handle` + `AlertDialog.createHandle<Payload>()`**: nominally typed apart from
  `Dialog.createHandle()`; otherwise identical semantics for reusing one confirmation
  instance across many trigger points (e.g. delete buttons throughout a list).
- **`actionsRef`**: `{ unmount, close }`, identical shape and purpose to Dialog's.

{/* END: general */}

{/* BEGIN: do-dont */}
## Do / Don't

**Do**

- Always render an `AlertDialog.Close` inside the Popup: it's the only pointer exit.
- Branch on `eventDetails.reason === 'escape-key'` in `onOpenChange` if Esc should be
  treated the same as an explicit Cancel for a given confirmation.
- Use AlertDialog for the one "are you sure?" decision point nested inside a larger,
  dismissible Dialog flow.

**Don't**

- Don't rebuild this with Dialog + manual "are you sure" state. AlertDialog already
  provides that pattern (no dismiss path, `role="alertdialog"`) with the correct
  ARIA semantics.
- Don't expect the backdrop or an outside click to ever close an AlertDialog: it's
  a permanent, type-level guarantee, not a runtime default that can be flipped.

{/* END: do-dont */}

{/* BEGIN: brand */}
## Brand guidelines

**Do**

- Float it: overlay shadow plus the sheet radius rung (16px). (BR-06, BR-11)

**Don't**

- Don't give the destructive action a red fill — red is error ink only; the missing danger-surface token is a recorded clash. (BR-02)
{/* END: brand */}

{/* BEGIN: examples */}
## In the wild

<WildCards component="alert-dialog">
  <WildCard
    repo="WordPress/gutenberg"
    title="WordPress Gutenberg"
    href="https://github.com/WordPress/gutenberg/blob/HEAD/packages/ui/src/alert-dialog/root.tsx"
    live="https://github.com/WordPress/gutenberg"
    license="no SPDX license detected"
    reuse="link-only, do not copy"
  >
    one file per Base UI part (root/popup/trigger/portal/context) plus its own test suite and
    Storybook story: a complete one-to-one part mapping.
  </WildCard>
  <WildCard
    repo="borabaloglu/9ui"
    title="9ui"
    href="https://github.com/borabaloglu/9ui/blob/HEAD/apps/www/src/components/ui/alert-dialog.tsx"
    live="https://www.9ui.dev"
    license="MIT"
    reuse="code-ok"
    image={nineUiDevShot}
  >
    a Tailwind-styled confirm/destructive-action wrapper, distributed as a copy-paste shadcn-style
    registry item.
  </WildCard>
  <WildCard
    repo="patrick-xin/lumi-ui"
    title="lumi-ui"
    href="https://github.com/patrick-xin/lumi-ui/blob/HEAD/packages/ui/src/components/ui/alert-dialog.tsx"
    live="https://www.lumiui.dev"
    license="MIT"
    reuse="code-ok"
    image={lumiuiDevOpenShot}
  >
    the same registry pattern, with a dedicated `alert-dialog-open-from-menu.tsx` example showing it
    triggered from a dropdown menu item rather than a plain button.
  </WildCard>
</WildCards>

{/* END: examples */}

{/* BEGIN: behavior */}
## FAQ

- **"Why doesn't clicking the backdrop close my alert dialog?"** → this is by design:
  `disablePointerDismissal` is permanently `true`; use `AlertDialog.Close` (or a
  controlled `onOpenChange` call from your own button) as the exit path.
- **"Esc still closes it even though I can't click outside. Is that a bug?"** → no.
  Outside-press is blocked, Esc is not. This is a known gap in our docs:
  [#1687](https://github.com/mui/base-ui/issues/1687) (open) asks for the three
  Dialog/AlertDialog differences (role, modal, pointer dismissal) to be stated on
  the official docs site; they still aren't.

{/* END: behavior */}

{/* BEGIN: when-to-use */}
## Related

- For everything dismissible, use [Dialog](?path=/docs/overlays-dialog--docs):
  same parts, but every axis Dialog exposes as configurable is fixed at its most
  conservative setting here.
- For contextual action lists, see [Menu](?path=/docs/overlays-menu--docs) or
  [Context Menu](?path=/docs/overlays-context-menu--docs). Opening an AlertDialog
  from a `Menu.Item`'s `onClick` is a documented composition (see Dialog's
  "Open from a menu" pattern, identical for AlertDialog).

{/* END: when-to-use */}

{/* BEGIN: props */}
## API reference

### AlertDialog.Root

<ArgTypes of={AlertDialog.Root} />

### AlertDialog.Trigger

<ArgTypes of={AlertDialog.Trigger} />

### AlertDialog.Portal

<ArgTypes of={AlertDialog.Portal} />

### AlertDialog.Backdrop

<ArgTypes of={AlertDialog.Backdrop} />

### AlertDialog.Viewport

<ArgTypes of={AlertDialog.Viewport} />

### AlertDialog.Popup

<ArgTypes of={AlertDialog.Popup} />

### AlertDialog.Title

<ArgTypes of={AlertDialog.Title} />

### AlertDialog.Description

<ArgTypes of={AlertDialog.Description} />

### AlertDialog.Close

<ArgTypes of={AlertDialog.Close} />

_Full JSDoc-driven reference: [base-ui.com/react/components/alert-dialog](https://base-ui.com/react/components/alert-dialog#api-reference)._

{/* END: props */}
