import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as AvatarStories from './avatar.stories';
import { Avatar } from '@base-ui/react/avatar';
import { WildCards, WildCard } from '../shared/InTheWild';
import nineUiDevShot from '../../../../../research/d-real-world-usage/_captures/9ui-dev.png';
import lumiuiDevOpenShot from '../../../../../research/d-real-world-usage/_captures/lumiui-dev-open.png';
import reuiIoOpenShot from '../../../../../research/d-real-world-usage/_captures/reui-io-open.png';

<Meta of={AvatarStories} />

# Avatar

{/* BEGIN: general */}
Displays a user's profile picture, initials, or fallback icon.

- **Import:** `import { Avatar } from '@base-ui/react/avatar';`
- **Parts:** 3: `Avatar.Root`, `Avatar.Image`, `Avatar.Fallback`.
- **Status:** Stable. Shipped 2025-02-04 ([#1210](https://github.com/mui/base-ui/pull/1210)), a community contribution by @acomanescu, referencing Radix UI's Avatar primitive.
- **ARIA pattern:** none. There is no dedicated W3C ARIA APG pattern for "Avatar"; it maps to the generic `img`-role semantics of a plain image, not a distinct composite pattern.

<Canvas of={AvatarStories.Hero} />

## Features

- **Off-DOM loading probe**: `useImageLoadingStatus` creates a detached `new window.Image()` to detect load status, so the real `<img>` never needs to exist in a "broken" state; `Avatar.Image` simply doesn't mount until status is `loaded`.
- **Automatic fallback on error or no source**: `Avatar.Fallback` renders whenever the image is missing, loading, or errored.
- **Anti-flash `delay`**: `Avatar.Fallback`'s `delay` prop (default `0`) defers showing the fallback, avoiding a flash for images that load fast; once shown without a delay, it stays shown (a one-way ratchet).
- **Cached-image fast path**: the loading hook synchronously checks `image.complete` after wiring the probe, resolving `loaded`/`error` immediately for already-cached images instead of waiting for the async `onload` event ([#4469](https://github.com/mui/base-ui/pull/4469)).

{/* END: general */}

{/* BEGIN: when-to-use */}
## When to use

- User profile pictures with a graceful fallback (initials or icon) when no image is set or the image fails to load.
- Identity display in lists/headers/chat UIs.
- Showing initials-as-placeholder when a photo isn't available; both hero avatars above default to this.
- Responsive/high-DPI image scenarios: `Avatar.Image` forwards native `srcSet`/`sizes`/`crossOrigin`/`referrerPolicy`, and the loading probe respects them too.

## When not to use

- For a purely decorative image with no need for load-state fallback, a plain `<img>` is simpler. Avatar's value is the loading-status state machine.
- For a numeric or measurement display (e.g. a completion ring, a presence dot), that's [Progress](?path=/docs/status-display-progress--docs) or [Meter](?path=/docs/status-display-meter--docs)'s job. Avatar has no numeric-measurement API; a presence indicator would need to be composed alongside it.
- For lazy-loaded image grids at scale, the off-DOM probe design forecloses `loading="lazy"`, a known open gap ([#2597](https://github.com/mui/base-ui/issues/2597)).

{/* END: when-to-use */}

{/* BEGIN: anatomy */}
## Anatomy

```tsx
import { Avatar } from '@base-ui/react/avatar';

<Avatar.Root>
  <Avatar.Image src="" />
  <Avatar.Fallback>LT</Avatar.Fallback>
</Avatar.Root>;
```

| Part              | Renders  | Purpose                                                         | Optional?                                                           |
| :----------------- | :-------- | :--------------------------------------------------------------- | :------------------------------------------------------------------- |
| `Avatar.Root`     | `<span>` | Container; owns the shared `imageLoadingStatus` context         | required                                                            |
| `Avatar.Image`    | `<img>`  | The image; only mounts once loading resolves to `loaded`        | yes; `Root` alone with text children is a valid "no photo" pattern |
| `Avatar.Fallback` | `<span>` | Rendered when the image fails to load, is loading, or is absent | yes; nothing shows in its place if omitted                         |

{/* END: anatomy */}

{/* BEGIN: behavior */}
## How it works

### The loading-status state machine

`ImageLoadingStatus` is `'idle' | 'loading' | 'loaded' | 'error'`, owned by `useImageLoadingStatus` inside `Avatar.Image`. No `src`/`srcSet` means immediate `error`; otherwise a detached probe resolves `loaded`/`error` asynchronously (or synchronously via the cached-image fast path). `Avatar.Fallback` renders whenever status is not `loaded` (respecting `delay`):

<Canvas of={AvatarStories.BrokenImageFallback} />

`onLoadingStatusChange` fires on every non-idle transition, independent of whether `Fallback` is even present:

<Canvas of={AvatarStories.OnLoadingStatusChangeCallback} />

### Anti-flash `delay`

With no `delay`, the fallback for a broken image appears the instant loading resolves to `error`. With `delay={600}` (the hero demo's value), the fallback is withheld for 600ms, so a fast-resolving load never flashes fallback content first.

<Canvas of={AvatarStories.DefaultDelay} />

### `alt` text: informative vs. decorative

<Canvas of={AvatarStories.AltTextVariations} />

### SSR and hydration

Because layout effects don't run during SSR, server-rendered HTML always contains `Avatar.Fallback` content, never `<img>`. Post-hydration, the cached-image fast path resolves `loaded` on the first render with no visible flash for browser-cached images. However, still-open issue [#4468](https://github.com/mui/base-ui/issues/4468) documents measured flash durations for cached images on SSR hydration across frameworks (TanStack Start ~304ms/14 frames, Next.js ~52ms/3 frames, Vite SPA ~16ms/1 frame). The same limitation applies to Radix's comparable fix.

{/* END: behavior */}

{/* BEGIN: a11y */}
## Keyboard & screen reader

**Keyboard:** none. Avatar is not interactive by itself; `Root`/`Image`/`Fallback` have no built-in role, `tabIndex`, or key handling. If used as a trigger (inside a button/menu), the wrapping interactive element owns focus/keyboard semantics.

- **ARIA**: no dedicated role is applied by Avatar itself beyond the native `<img>`'s implicit image semantics.
- **You must provide**: `alt` text on `Avatar.Image`. **Our docs don't yet give this guidance, and both hero demos omit `alt`**. Decide decorative (`alt=""`, name shown as adjacent text) vs. informative (real alt text, the avatar is the sole identifier) per context; informative is the intended default.
- **`Avatar.Fallback` is not hidden from assistive tech**: fallback text such as initials stays in the accessibility tree, so add `aria-hidden` yourself when the fallback is purely decorative.

{/* END: a11y */}

{/* BEGIN: styling */}
## Styling hooks

- `Avatar.Image` exposes `data-starting-style`/`data-ending-style` during transition-in/-out.
- **No `data-loading-status`/`data-loaded`/`data-error` attribute exists on any part.** `avatarStateAttributesMapping` maps `imageLoadingStatus: () => null`: the state is tracked in React but deliberately not exposed as a data attribute. Style by which part is mounted (`Image` vs. `Fallback`), or via the `className`/`style` state-function form reading `state.imageLoadingStatus`.

{/* END: styling */}

{/* BEGIN: general */}
## Choosing the right props

- **`src` / `srcSet` / `sizes` (Image)**: the image URL(s); omitting both `src` and `srcSet` forces immediate `error`. Responsive props are forwarded to both the rendered `<img>` and the internal probe so status reflects the real resource.
- **`crossOrigin` / `referrerPolicy` (Image)**: forwarded to the probe so CORS/referrer behavior matches the real `<img>`, keeping status detection accurate for cross-origin sources.
- **`alt` (Image)**: native passthrough, typed but not required by the type system; see the a11y gap above.
- **`onLoadingStatusChange` (Image)**: fires on every non-idle transition; use for custom side effects (analytics, a distinct spinner) without affecting rendering.
- **`delay` (Fallback, default `0`)**: use a nonzero value (the hero demo uses `600`ms) to avoid a flash-of-fallback for typically-fast image loads; once shown, Fallback stays shown even if `delay` later changes.

{/* END: general */}

{/* BEGIN: do-dont */}
## Do / Don't

**Do**

- Set `alt` explicitly on every `Avatar.Image` and decide decorative vs. informative per context; our docs don't yet prescribe a rule, so make the decision consciously.
- Use `delay` on `Avatar.Fallback` to avoid flash-of-fallback for fast-loading images.

**Don't**

- Don't assume `Avatar.Fallback` is hidden from screen readers. It isn't; this is by design ([#3095](https://github.com/mui/base-ui/pull/3095)). Add `aria-hidden` yourself if it's purely decorative.
- Don't rely on `loading="lazy"` for large avatar grids: the off-DOM probe design forecloses it ([#2597](https://github.com/mui/base-ui/issues/2597)).

{/* END: do-dont */}

{/* BEGIN: brand */}
## Brand guidelines

**Do**

- Use the fully-round radius rung for the shape. (BR-11)
- Keep fallback initials neutral ink on a neutral surface — no colored avatar backgrounds. (BR-01)
{/* END: brand */}

{/* BEGIN: examples */}
## In the wild

<WildCards component="avatar">
  <WildCard
    repo="borabaloglu/9ui"
    title="9ui"
    href="https://github.com/borabaloglu/9ui/blob/HEAD/apps/www/src/components/ui/avatar.tsx"
    live="https://www.9ui.dev"
    license="MIT"
    reuse="code-ok"
    image={nineUiDevShot}
  >
    a Tailwind wrapper with dedicated `avatar-group-demo` and `avatar-with-fallback-demo` examples
    showing the fallback slot in use.
  </WildCard>
  <WildCard
    repo="patrick-xin/lumi-ui"
    title="lumi-ui"
    href="https://github.com/patrick-xin/lumi-ui/blob/HEAD/packages/ui/src/components/ui/avatar.tsx"
    live="https://www.lumiui.dev"
    license="MIT"
    reuse="code-ok"
    image={lumiuiDevOpenShot}
  >
    the same registry-item pattern, published as both a package export and a standalone installable
    file.
  </WildCard>
  <WildCard
    repo="keenthemes/reui"
    title="reui"
    href="https://github.com/keenthemes/reui/blob/HEAD/registry/bases/base/ui/avatar.tsx"
    live="https://reui.io"
    license="MIT"
    reuse="code-ok"
    image={reuiIoOpenShot}
  >
    one of the components reui ships with interchangeable Base UI and Radix backends behind an
    identical public API.
  </WildCard>
</WildCards>

{/* END: examples */}

{/* BEGIN: history */}
## Decision log

- **2025-01-20 → 2025-02-04**: Avatar ships (issue [#1347](https://github.com/mui/base-ui/issues/1347) → PR [#1210](https://github.com/mui/base-ui/pull/1210)), a community contribution by @acomanescu, referencing Radix UI's Avatar.
- **2026-02-11**: transition attributes added, `data-starting-style`/`data-ending-style` ([#3939](https://github.com/mui/base-ui/pull/3939)).
- **2026-02-16**: premature-mount bug fixed: Image no longer mounts before fully loaded ([#4110](https://github.com/mui/base-ui/pull/4110)).
- **2026-03**: community `aria-hidden`-by-default proposal for Fallback closed without merging ([#3095](https://github.com/mui/base-ui/pull/3095)).
- **2026-04-09**: cached-image flash fix: `image.complete` fast path added ([#4469](https://github.com/mui/base-ui/pull/4469), addressing still-open [#4468](https://github.com/mui/base-ui/issues/4468)).
- **2026-07-02**: fallback `delay={0}` default clarified to mean immediate, no wait ([#5147](https://github.com/mui/base-ui/pull/5147)).

{/* END: history */}

{/* BEGIN: known-issues */}
## Known issues & open questions

- [#4468](https://github.com/mui/base-ui/issues/4468) (open): fallback still flashes for cached images on SSR hydration in measurable, framework-dependent durations, despite the cached-image fast path.
- [#2597](https://github.com/mui/base-ui/issues/2597) (open): no lazy-loading support, an architectural consequence of the off-DOM probe design.
- **`alt` guidance gap**: a known gap in our docs with no tracking issue yet; neither hero demo sets `alt`, and the docs give no decorative-vs-informative rule.

{/* END: known-issues */}

{/* BEGIN: when-to-use */}
## Related

- For a numeric measurement or task-completion display, see [Progress](?path=/docs/status-display-progress--docs) / [Meter](?path=/docs/status-display-meter--docs): same taxonomy category, different job.

{/* END: when-to-use */}

{/* BEGIN: props */}
## API reference

### Avatar.Root

<ArgTypes of={Avatar.Root} />

### Avatar.Image

<ArgTypes of={Avatar.Image} />

### Avatar.Fallback

<ArgTypes of={Avatar.Fallback} />

_Full JSDoc-driven reference: [base-ui.com/react/components/avatar](https://base-ui.com/react/components/avatar#api-reference)._

{/* END: props */}
