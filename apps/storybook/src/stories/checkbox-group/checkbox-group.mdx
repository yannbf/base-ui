import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as CheckboxGroupStories from './checkbox-group.stories';
import { CheckboxGroup } from '@base-ui/react/checkbox-group';
import { Checkbox } from '@base-ui/react/checkbox';
import { WildCards, WildCard } from '../shared/InTheWild';
import nineUiDevShot from '../../../../../research/d-real-world-usage/_captures/9ui-dev.png';
import lumiuiDevOpenShot from '../../../../../research/d-real-world-usage/_captures/lumiui-dev-open.png';
import cossComUiShot from '../../../../../research/d-real-world-usage/_captures/coss-com-ui.png';

<Meta of={CheckboxGroupStories} />

# Checkbox Group

{/* BEGIN: general */}
Turns N independently-toggleable checkboxes into one array-valued, form-participating field: the direct multi-select counterpart to [Radio Group](?path=/docs/form-inputs-radio--docs)'s single-value coordination.

- **Import:** `import { CheckboxGroup } from '@base-ui/react/checkbox-group';`
- **Parts:** 1 (a single default export `CheckboxGroup`; no namespace, no sub-parts of its own). Composes with the separately-exported `Checkbox.Root`/`Checkbox.Indicator`.
- **Status:** Stable. Introducing commit `9c04f2c3e`, "[CheckboxGroup] Implement components and hooks" (`#458`).
- **ARIA pattern:** No single named APG composite pattern. The group renders `role="group"` around N independent [Checkbox](https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/) widgets, each following the plain Checkbox pattern individually.

<Canvas of={CheckboxGroupStories.Basic} />

## Features

- **Submits as one named array field**: the motivating use case (`#1948`). Checking two of three checkboxes submits `["a","b"]` under a single field name, once wrapped in `Field.Root`.
- **A built-in "select all" parent-checkbox pattern**: a dedicated `useCheckboxGroupParent` hook, its own test file, and a cluster of maintenance PRs hardening it against edge cases.
- **The parent-click cycle is a specific, tested algorithm**: clicking a parent in a mixed state (some but not all children checked) follows `mixed → on → off → mixed`, and the final step **restores the exact prior subset** rather than resetting to empty.
- **No arrow-key composite navigation**: unlike Radio Group's `role="radiogroup"` + roving tabindex, Checkbox Group renders a plain `<div role="group">`; each checkbox is its own independent Tab stop.

{/* END: general */}

{/* BEGIN: when-to-use */}
## When to use

- Several related checkboxes whose checked states should coordinate as **one named, array-valued form field**.
- Any set of checkboxes needing a "select all" / parent-child summary control, including nested groups (app-composed via two `CheckboxGroup` instances with shared `onValueChange` propagation; there is no native multi-level tree API).
- Grouping labeling: `Field.Item` wraps each option, `Fieldset.Legend` labels the whole group (identical composition to Radio Group).

## When not to use

- **Large or filterable option sets that need to submit as an array** → prefer a Combobox in `multiple` mode. We declined building a checkbox tree _inside_ Combobox: trees are out of Combobox's scope, and CheckboxGroup cannot be combined with it (`mui/base-ui#2695`). A modified multiple-select Combobox demo with its own select-all item is the recommended alternative.
- **A tree/hierarchical multi-select with nested select-all at arbitrary depth** → not built in anywhere; the closest pattern is the app-composed two-instance nesting above, and we declined the Combobox-tree request (`#2695`).
- **An exclusive, always-exactly-one choice** → [Radio Group](?path=/docs/form-inputs-radio--docs), not Checkbox Group.
- **A single independent checkbox that never needs to coordinate with siblings** → standalone [Checkbox.Root](?path=/docs/form-inputs-checkbox--docs), no group wrapper needed.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}
## Anatomy

```tsx
import { Checkbox } from '@base-ui/react/checkbox';
import { CheckboxGroup } from '@base-ui/react/checkbox-group';

<CheckboxGroup>
  <Checkbox.Root />
  {/* …repeated per option, each with its own value */}
</CheckboxGroup>;
```

| Part                    | Renders                                  | Purpose                                                                              | Optional?                |
| :----------------------- | :---------------------------------------- | :------------------------------------------------------------------------------------ | :------------------------ |
| `CheckboxGroup`         | `<div role="group">`                     | Shared array-valued state for a series of checkboxes                                 | required                 |
| `Checkbox.Root` (child) | `<span role="checkbox">`                 | One independently-toggleable option, contributing its `value` to the group's array   | required, one per option |
| `Checkbox.Root parent`  | `<span role="checkbox">` + `data-parent` | The "select all" coordinator; `name` is force-unset so it's excluded from submission | optional                 |

A parent checkbox's `aria-controls` is a space-separated list of every child's id, derived from the group's id scheme; setting a custom `id` on a child breaks this (`mui/base-ui#2691`).

{/* END: anatomy */}

{/* BEGIN: behavior */}
## How it works

### Uncontrolled vs controlled

`defaultValue`/`value`/`onValueChange` (all `string[]`), with `defaultValue` defaulting to `[]` when omitted. Each child's click independently pushes/removes its `value` from the array (filter-based removal, not a re-sort).

<Canvas of={CheckboxGroupStories.GroupValueCoordination} />

### The parent "select all" tri-state cycle

<Canvas of={CheckboxGroupStories.ParentCheckboxTriState} />

The parent's `checked`/`indeterminate` are computed automatically from the live child array (`checked = value.length === allValues.length`, `indeterminate` when some but not all are checked), unless manually overridden. Clicking the parent when all-or-nothing is checked simply flips select-all/select-none. But from a **mixed** state, the click cycle is `mixed → on → off → mixed`, and the final click restores exactly the subset that was checked before, not an empty set. Clicking the parent fires only the parent's own `onCheckedChange`; it does **not** fire each child's individual callback.

Requires `allValues` (every child's value) and a **controlled** `value` + `onValueChange`. The parent pattern is not supported in fully-uncontrolled mode.

<Canvas of={CheckboxGroupStories.ParentIndeterminateFromPartialSelection} />

<Canvas of={CheckboxGroupStories.DisabledChildExcludedFromParentToggle} />

A `disabled` child is excluded from the parent's bulk select-all/select-none: it is left untouched by either bulk action, regardless of its own checked state.

### Nested groups (app-composed)

<Canvas of={CheckboxGroupStories.NestedParentCheckbox} />

There is no native multi-level tree API. A nested "group of groups" is composed by wiring two `CheckboxGroup` instances together with app-level `onValueChange` propagation, as in the docs' "nested" demo.

### Form integration

<Canvas of={CheckboxGroupStories.FormSubmitArray} />

`CheckboxGroup` has no `name` prop of its own. Wrap it in `Field.Root name` (composed with `Fieldset.Root render={<CheckboxGroup />}`) to submit the checked values as one array-valued field (`#1948`). No per-checkbox `name` is needed.

### The counter-intuitive `required` contract

<Canvas of={CheckboxGroupStories.RequiredMeansAllMustBeChecked} />

Setting `required` on individual group members means **every** `required` checkbox must be checked before the group's error clears; it does **not** mean "at least one must be checked," which is what "required" might intuitively suggest for a multi-select set. If "at least one" is the desired semantics, implement it via a custom `Field.Root validate` function reading the group's array length. There is no built-in prop for it.

{/* END: behavior */}

{/* BEGIN: a11y */}
## Keyboard & screen reader

| Key     | Context                       | Action                                                                                                                 |
| :------- | :----------------------------- | :---------------------------------------------------------------------------------------------------------------------- |
| `Tab`   | anywhere in the group         | Moves focus to the **next individual checkbox**; each is its own Tab stop, unlike Radio Group's single composite stop |
| `Space` | a focused child checkbox      | Toggles that checkbox, pushing/removing its `value` from the group array                                               |
| `Space` | a focused **parent** checkbox | Cycles the tri-state select-all/select-none/restore-mixed algorithm                                                    |
| `Enter` | any focused checkbox          | Submits the owning form, does not toggle (identical to standalone Checkbox)                                            |

- **ARIA**: `role="group"`, `aria-labelledby` from `Field.Label`/`Fieldset.Legend`. No `aria-required`/`aria-disabled` of its own; those cascade individually to children, unlike Radio Group which sets them on the group container itself.
- **You must provide**: a group label (`Fieldset.Legend` or `aria-labelledby`) and a label for each option, identical to standalone Checkbox.
- **Known issues**: a custom `id` set on a child inside a parent-coordinated group silently breaks the parent's derived `aria-controls` (`mui/base-ui#2691`, noted in a source `TODO`).

{/* END: a11y */}

{/* BEGIN: styling */}
## Styling hooks

The group container itself carries only `data-disabled`, plus the full Field validity set (`data-valid`/`data-invalid`/`data-touched`/`data-dirty`/`data-filled`/`data-focused`). It has no `data-checked`/`data-unchecked`/`data-indeterminate` of its own, since "checked" isn't meaningful for the container. Each child (and optional parent) carries the full `Checkbox.Root`/`Checkbox.Indicator` attribute set; see [Checkbox](?path=/docs/form-inputs-checkbox--docs#styling-hooks).

{/* END: styling */}

{/* BEGIN: general */}
## Choosing the right props

- **`allValues`**: required for the parent-checkbox pattern to compute its tri-state correctly; must be paired with a controlled `value`+`onValueChange`.
- **`disabled` (group-level)**: cascades to every child and takes precedence over any individual child's own `disabled={false}`; disabled children are excluded from the parent's bulk select-all/select-none computation.
- **`required` (per-child, not group-level)**: see the counter-intuitive contract above; there is no group-level prop meaning "at least one selected."

{/* END: general */}

{/* BEGIN: do-dont */}
## Do / Don't

**Do**

- Wrap in `Field.Root name` when the array-valued result needs to reach native `FormData` as one field.

**Don't**

- Don't rely on individual child `required` to mean "pick at least one": it means "every required one must be checked." Use a custom `validate` function instead.
- Don't set a custom `id` on a child inside a parent-coordinated group; it breaks the parent's `aria-controls`.

{/* END: do-dont */}

{/* BEGIN: brand */}
## Brand guidelines

**Do**

- Reach for a checkbox group only when several options can be on at once — an exclusive choice is a radio group. (DS-04)
{/* END: brand */}

{/* BEGIN: examples */}
## In the wild

<WildCards component="checkbox-group">
  <WildCard
    repo="borabaloglu/9ui"
    title="9ui"
    href="https://github.com/borabaloglu/9ui/blob/HEAD/apps/www/src/components/ui/checkbox-group.tsx"
    live="https://www.9ui.dev"
    license="MIT"
    reuse="code-ok"
    image={nineUiDevShot}
  >
    a dedicated wrapper file separate from plain Checkbox, confirming CheckboxGroup is treated as
    its own registry item rather than folded into Checkbox.
  </WildCard>
  <WildCard
    repo="patrick-xin/lumi-ui"
    title="lumi-ui"
    href="https://github.com/patrick-xin/lumi-ui/blob/HEAD/packages/ui/src/components/ui/checkbox-group.tsx"
    live="https://www.lumiui.dev"
    license="MIT"
    reuse="code-ok"
    image={lumiuiDevOpenShot}
  >
    the same split (a standalone CheckboxGroup registry item alongside Checkbox), published as both
    a package export and an installable file.
  </WildCard>
  <WildCard
    repo="cosscom/coss"
    title="coss"
    href="https://github.com/cosscom/coss/blob/HEAD/packages/ui/src/components/checkbox-group.tsx"
    live="https://coss.com/ui"
    license="AGPL-3.0"
    reuse="link-only, do not copy"
    image={cossComUiShot}
  >
    Cal.com's registry keeps CheckboxGroup separate from Checkbox as well, the same pattern in
    two independent registries.
  </WildCard>
</WildCards>

{/* END: examples */}

{/* BEGIN: history */}
## Decision log

- **(`#458`)**: Checkbox Group ships as "[CheckboxGroup] Implement components and hooks."
- **(`#1948`)**: "Enable submitting checkbox group value as one field" establishes the array-as-one-named-field submission contract, the component's core use case.
- **2025-11 (`#3205`, shared scope)**: Root renders `<span>` not `<button>`, applying identically to every checkbox in the group.
- **2026 (`#4535`)**: "Fix uncontrolled default initialization", an array-valued-specific initialization edge case.
- **2026 (`#4660`)**: "Remove `index.parts.ts` from Checkbox Group", a cleanup that settled the single-part framing.
- **2026 (`#4912`, `#4941`)**: Parent-checkbox hardening: "Fix parent checkbox with custom `validate` fn" and "Fix parent group cancellation and indeterminate state."
- **2026 (`#4958`)**: "Fix validation with multiple required checkboxes" hardens the all-required-must-be-checked contract.
- **`#2695` (declined)**: A user asked for a tree of grouped checkboxes with a "select all" root item _inside Combobox_; we declined, keeping the parent-checkbox pattern scoped to Checkbox Group's flat list rather than generalizing it into a tree widget.

{/* END: history */}

{/* BEGIN: when-to-use */}
## Related

- For an exclusive, always-exactly-one choice, use [Radio Group](?path=/docs/form-inputs-radio--docs).
- For a single independent checkbox with no group coordination, use [Checkbox](?path=/docs/form-inputs-checkbox--docs).
- To add labels, descriptions, and validation, wrap in [Field](?path=/docs/form-inputs-field--docs).

{/* END: when-to-use */}

{/* BEGIN: props */}
## API reference

### CheckboxGroup

<ArgTypes of={CheckboxGroup} />

### Checkbox.Root

<ArgTypes of={Checkbox.Root} />

_Full JSDoc-driven reference: [base-ui.com/react/components/checkbox-group](https://base-ui.com/react/components/checkbox-group#api-reference)._

{/* END: props */}
