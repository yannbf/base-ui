import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentBrowser } from '../shared/ComponentBrowser';

# Component browser

<Meta title="Overview/Component browser" />

Every Base UI component documented in this Storybook, grouped by category. Type to filter by name or description, and
click a card to open that component's docs page, which leads with its primary story.

<ComponentBrowser />
